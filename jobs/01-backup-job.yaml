apiVersion: batch/v1
kind: Job
metadata:
  name: manual-backup
  namespace: k8s-lifecycle-prod
spec:
  template:
    spec:
      containers:
      - name: backup
        image: busybox
        command: ["echo", "Бэкап базы данных выполнен успешно!"]
      restartPolicy: OnFailure
